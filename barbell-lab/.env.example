# Copy to .env (gitignored). NEVER commit real keys.
FMP_API_KEY=            # cross-provider validation feed (same key the genomics service uses)
FRED_API_KEY=           # optional — public fredgraph.csv fallback works without it
TIINGO_API_KEY=         # optional third price feed

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic-settings>=2.2
httpx>=0.27
# PINNED (2026-08-26): a live-money venue adapter must not re-resolve its
# SDK on every image rebuild. The unpinned '>=1.7' meant every deploy could
# silently change the API surface under us; the phantom-position incident's
# fallback called a method that existed in NO version, and nothing failed
# until it ran against real money. A gate test asserts every method cb.py
# calls exists on RESTClient at this exact version.
coinbase-advanced-py==1.8.4

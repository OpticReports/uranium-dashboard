# Verifier code (record only)

Each folder is the code one adversarial verifier wrote to recompute or audit a block, run in
the study sandbox on 2026-09-15/16. Paths inside point at that sandbox
(`/tmp/.../scratchpad/oil`), so they are kept as a record of what was checked, not as
re-runnable scripts; the verdicts are logged in `../../oil-shock-recession-weight.md` §8
entry 2. The blocks' own `run.py` files under `../blocks/` ARE re-runnable from this folder.

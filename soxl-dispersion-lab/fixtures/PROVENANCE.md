# Fixture provenance — SOXL dispersion study

Frozen by fetch_data.py. Downstream scripts are offline.

```
# SOXL-dispersion fixture freeze — fetched 2026-08-14
# vendor: FMP stable endpoints; window 2009-01-01 -> 2026-08-14
  ... 20/108 tickers
  ... 40/108 tickers
  ... 60/108 tickers
  REUSE-GUARD ALTR: series runs to 2025-03-26 but company died 2015-12-31 -> dropped 1859 post-death rows
  THIN ALTR: only 0 adj rows -> excluded
  ... 80/108 tickers
  THIN IDTI: only 0 adj rows -> excluded
  REUSE-GUARD IIVI: series runs to 2023-06-30 but company died 2022-09-30 -> dropped 187 post-death rows
  REUSE-GUARD IIVI: series runs to 2023-06-30 but company died 2022-09-30 -> dropped 187 post-death rows
  THIN IRF: only 0 adj rows -> excluded
  THIN ISIL: only 0 adj rows -> excluded
  THIN ISSI: only 0 adj rows -> excluded
  REUSE-GUARD NANO: series runs to 2020-11-16 but company died 2019-10-31 -> dropped 1 post-death rows
  THIN NANO: only 0 adj rows -> excluded
  THIN NVLS: only 0 adj rows -> excluded
  THIN PLXT: only 0 adj rows -> excluded
  THIN RFMD: only 0 adj rows -> excluded
  THIN SIMG: only 0 adj rows -> excluded
  REUSE-GUARD SNDK: series runs to 2026-08-14 but company died 2016-05-31 -> dropped 377 post-death rows
  THIN SNDK: only 0 adj rows -> excluded
  THIN TQNT: only 0 adj rows -> excluded
  THIN VSEA: only 0 adj rows -> excluded
  THIN VTSS: only 0 adj rows -> excluded
  treasury 3m: 547 obs 2010-10-04 -> 2026-08-13
  SOXX live holdings: 33 lines (reconstruction check)
  wrote /home/user/uranium-dashboard/soxl-dispersion-lab/fixtures/market_data.json (7.8 MB), 94 price series, 94 mcap series
```
